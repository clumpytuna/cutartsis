server {
  listen 80 default_server;
  root /usr/share/nginx/html;

  location / {
    rewrite ^ /index.html break;
  }

  location /static {
    alias /usr/share/nginx/html;
    expires max;
    gzip on;
    gzip_types *;
  }
}
