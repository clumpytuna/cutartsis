version: '3'

services:
  frontend:
    image: cutartsis-frontend
    restart: always
    ports:
      - 80:80
      - 443:443
    volumes:
      - /data/frontend-images/pictures_processed/:/images
      - /data/cache/:/cache
      - /root/certs-data/:/data/letsencrypt/
      - /etc/letsencrypt/:/etc/letsencrypt/
    container_name: frontend

